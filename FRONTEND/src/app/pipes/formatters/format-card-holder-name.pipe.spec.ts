import { FormatCardHolderNamePipe } from './format-card-holder-name.pipe';

describe('FormatCardHolderNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardHolderNamePipe();
    expect(pipe).toBeTruthy();
  });
});
