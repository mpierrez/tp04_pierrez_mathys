import { FormatCardYearPipe } from './format-card-year.pipe';

describe('FormatCardYearPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardYearPipe();
    expect(pipe).toBeTruthy();
  });
});
