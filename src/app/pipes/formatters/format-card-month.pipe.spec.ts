import { FormatCardMonthPipe } from './format-card-month.pipe';

describe('FormatCardMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
